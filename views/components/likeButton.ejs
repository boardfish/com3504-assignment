<div class="fivestars-group" class="d-flex align-items-center">
  <% Array(5).fill('★').forEach((star, index) => { %>
    <span class="fivestars" data-post-id="<%= postId %>" data-value="<%= index + 1 %>">
      <%= star %>
    </span>
  <% }) %>
</div>